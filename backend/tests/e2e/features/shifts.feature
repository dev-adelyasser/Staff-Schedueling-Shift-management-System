# backend/tests/e2e/features/shifts.feature
#
# Gherkin scenarios for the Shift Scheduling slice (UC-02, UC-03, UC-06).
#
# ⚠️  AI DOES NOT WRITE GHERKIN SCENARIOS.
#
# These scenarios must be authored by the team from the official requirements
# document so they match the actual acceptance criteria for each use case.
# Placeholder structure only — replace with real scenarios before submission.
#
# Suggested use cases to cover:
#   UC-02 — Create Shift (happy path, validation failure, DB unavailable)
#   UC-03 — View / Filter Shifts (staff sees only non-deleted; pagination)
#   UC-06 — Clock In / Clock Out
#
# Example skeleton (DO NOT submit with placeholder text):
#
# Feature: Shift Scheduling
#
#   Background:
#     Given the application is running
#     And the database is clean
#
#   Scenario: [TODO — UC-02 Create Shift happy path]
#     Given ...
#     When ...
#     Then ...
#
#   Scenario: [TODO — UC-02 Create Shift with end_time before start_time]
#     Given ...
#     When ...
#     Then ...
#
#   Scenario: [TODO — UC-03 Staff views paginated shift list]
#     Given ...
#     When ...
#     Then ...
